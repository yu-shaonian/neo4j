# D:/self_study/python
# —*- coding:utf-8 -*-
# 创建者：雷国军

if __name__ == "__main__":
    # -*- coding:utf-8 -*-
    import xlrd

    # data = xlrd.open_workbook('./lawdata/money.xls')  # 打开xls文件
    # table = data.sheets()[0]
    # nrows = table.nrows  # 获取表的行数
    # for i in range(nrows):  # 循环逐行打印
    #     # 跳过第一行
    #     if i == 0:
    #         continue
    #     demo = table.row_values(i)
    #     print(table.row_values(i))
    print(len("暂时没找到"))

    str_test = "hello{}world".format('guojun')
    print(str_test)